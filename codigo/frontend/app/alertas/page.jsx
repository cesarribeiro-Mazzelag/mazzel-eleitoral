"use client";
/**
 * /alertas - plataforma antiga (roxa). Conteudo extraido pra components/modulos/
 * pra ser reaproveitado tambem em /mazzel-preview/alertas.
 */
import { AppLayout } from "@/components/layout/AppLayout";
import { AlertasContent } from "@/components/modulos/AlertasContent";

export default function AlertasPage() {
  return (
    <AppLayout>
      <AlertasContent />
    </AppLayout>
  );
}
